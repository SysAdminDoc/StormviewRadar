{
  "name": "stormview-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:level2": "node scripts/build-level2-worker.mjs",
    "test": "npm run test:static && npm run test:unit && playwright test",
    "test:static": "npm run build:level2 && node --check src/mesh-analysis.js && node --check src/mesh-worker.js && node scripts/check-version.mjs && node scripts/check-vendor.mjs && node scripts/check-static.mjs",
    "test:unit": "node --test tests/*.test.js",
    "release:check": "npm test && npm audit --audit-level=high"
  },
  "dependencies": {
    "buffer": "6.0.3",
    "leaflet": "1.9.4",
    "nexrad-level-2-data": "2.4.3",
    "topojson-client": "3.1.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.0",
    "esbuild": "0.25.6"
  }
}
