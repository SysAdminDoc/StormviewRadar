{
  "name": "stormview-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:static && playwright test",
    "test:static": "node scripts/check-static.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "1.62.0"
  }
}
