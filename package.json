{
  "name": "stormview-radar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:static && playwright test",
    "test:static": "node scripts/check-version.mjs && node scripts/check-vendor.mjs && node scripts/check-static.mjs",
    "release:check": "npm test && npm audit --audit-level=high"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "topojson-client": "3.1.0"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.0"
  }
}
